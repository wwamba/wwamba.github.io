const form=document.querySelector("#user-subscribe");form.addEventListener('submit',(e)=>{e.preventDefault();ValidateEmail(form.inputEmail.value);document.getElementById('inputEmail').value=''});function ValidateEmail(mail){var user_email=mail.toLowerCase();if(/^\w+([\.-]?\w+)*@\w+([\.-]?\w+)*(\.\w{2,3})+$/.test(user_email)){uploadEmail(user_email);alert("Thank you! You have successfully subscribed!");return(!0)}
alert("You have entered an invalid email address!")
return(!1)};function uploadEmail(mail){db.ref('/subscription-entries/').push().set({email:mail})};function isEmailExist(mail){var rootRef=db.ref('/subscription-entries/');rootRef.orderByChild("email").equalTo(mail).on("child_added",function(snapshot){output="exists in db"})}